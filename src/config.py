# Drawing space
WIN_HEIGHT = 600
WIN_WIDTH = 600

# Recording
DELTA = 0.01     # used for approximating integral f(t), the smaller the better but makes programme slower
INTERVAL = 0.01  # time interval per recorded activity

# Replaying
VECTORS = 300     # non-inclusive of c_0
REPLAY_SPEED = 0.1
